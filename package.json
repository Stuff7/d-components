{
  "name": "d-components",
  "private": true,
  "workspaces": ["app", "lib"],
  "scripts": {
    "dev": "yarn workspace app dev",
    "upg:app": "yarn workspace app upgrade-lib",
    "preview": "yarn workspace app preview",
    "clean:lib": "yarn workspace lib clean",
    "build:app": "yarn workspace app build",
    "build": "yarn workspace lib build",
    "watch:lib": "yarn workspace lib watch"
  }
}
